#!/usr/bin/env bash
# This script groups visitors by IP and HTTP status code and displays the data

# Use awk to process the log file and print the occurrence number, IP, and HTTP code
awk '{ count[$1" "$9]++ } END { for (key in count) print count[key], key }' access.log | sort -nrk1

